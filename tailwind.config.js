/** @type {import('tailwindcss').Config} */
module.exports = {
  mode: 'jit', // Enable JIT mode
  purge: ['./**/*.html', './**/*.php'], // Add the paths to your HTML or PHP files
  theme: {
    extend: {},
  },
  plugins: [],
};
